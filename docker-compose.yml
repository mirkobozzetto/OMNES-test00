services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - sqlite-data:/app/packages/backend/prisma
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=file:./prisma/dev.db
    restart: unless-stopped

volumes:
  sqlite-data:
